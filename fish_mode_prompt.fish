# This is handled inside fish_mode_prompt (see `__dollar_prompt_vi`)
#
# If you want to override this with your own mode prompt, disable dollar's
# built-in mode prompt in your fish config:
#
#     set -g theme_display_vi no

function fish_mode_prompt; end
